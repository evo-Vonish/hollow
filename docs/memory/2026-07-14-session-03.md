# 会话记忆 · Session 03 · 2026-07-14

> 承接 session-02(2026-07-07)。分支 `claude/new-project-setup-1dzma1`,全部已提交推送。
> 本会话把 /v1 从"能跑"推向"能用/趋近可上线",并做了一轮 37-agent 对抗审计 + 逐批修复。

## 一、本会话交付(提交,新→旧)

- **工程成熟度打磨**(据可用度评估的"最短上线路径",用户"继续打磨"期间):
  - `da1c2fe` 集成测试:`tests/integration/test_api_live.py` 固化活网关验收(@pytest.mark.integration,
    CI 跳过,`HOLLOW_TEST_LIVE=1` 跑),断言不变量/契约。活测 17 passed。
  - `c453290` 部署产物:`deploy/` systemd×2 + nginx(SSE 关缓冲)+ gateway.env.example + `docs/design/07-deployment.md`
    (Linux 场景 B 指南 + "四个必做":锁依赖✓/目标环境冒烟/阈值校准/egress 防火墙)。
  - `fec9de2` 测试安全网:`tests/` **297 网络无关单元**(5-agent workflow 写+自验,未发现源码 bug)+ `pytest.ini`
    + `.github/workflows/ci.yml`(sparse-checkout api/tests,py3.12)+ requirements 锁版本(curl_cffi/starlette 提为显式)。
  - **可用度评估结论**(答用户):骨架优秀但非"稳定产品";三硬伤=底线④阈值未校准、目标环境零验证、(已补)零测试/CI。
    分场景:vonish 本地后端≈现在可试;单机 VPS≈补目标环境冒烟+校准即可;公网 SaaS 数月级(进程内限流需挪 Redis)。
- **生产就绪 4 批**(审计"生产就绪+产品差距"逐批修,每批实测 + 提交):
  - `3659c4d` 批4 产品差距:分页 `page`(SearXNG pageno)+ `include_domains`/`exclude_domains`(`api/filters.py`,
    子域匹配,召回后过滤)+ `usage` 账目(配额强制需存储,只暴露用量)。实测 10/10(page1/page2 零重叠)。
  - `90895e9` 批3 契约健壮:404/405/500 走统一错误封套(exception handlers)+ SSE 心跳(`: heartbeat`,常驻
    __anext__ 任务超时不 cancel)+ reconcile 假阳性拆 `engines_no_results`(不再误标 silent failure)。8/8。
  - `0b610b6` 批2 资源治理:`SEARX_MAX_CONCURRENCY=4` 上游闸(防 12 并发打熔断)+ 浏览器每请求闸 2→1(< 全局 2)
    + `MAX_INFLIGHT_HEAVY=8` 重端点在飞上限超限 429 shed-load。实测 12 并发→8×200+4×429。
  - `b91f5a8` 批1 可观测性:`api/logging_setup.py` 结构化日志 + access-log 中间件 + run_local 输出落 logs\*.log
    (不再 -WindowStyle Hidden 吞掉)+ /healthz?deep=1 接 SearXNG /stats/errors。底线③运维溯源落地。
  - **诚实分诊**:审计那份 harvest 在修复之前,SearXNG 400→502 / 缺 /v1/fetch / publishedDate 三项**早已修**,实测确认。
- `81d7726` **搜索字段补齐**(据 design/06 横评):`published_date`(SearXNG 透传,`orchestrator._published`,缺 null)
  + `highlights`/`highlight_scores`(`rerank.highlights()` 从全文抽 query 最相关 top-K 句,复用词汇打分、无模型,对齐 Exa)。
  `summary` 需 LLM 暂不做。snake_case 非照抄 Exa camelCase。实测单元 10/10 + 端到端 7/7。
- `152019a` **docs/design/06 搜索 API 格式横评**:实时拉 Tavily/Exa/Brave/Google/Serper 官方文档钉字段名。结论:
  搜索 API 分 SERP 派(Google/Bing/Brave/Serper)与 LLM 原生派(Tavily/Exa),都不是 OpenAI choices/message 形状;
  hollow = OpenAI 信封 + B 派结果体,方向对,且 score+relevance 双分/engine 溯源/账目细度比多数厂强。Perplexity 是唯一
  OpenAI 形状但它是答案 API。反直觉:Tavily 响应无 published_date(仅入参),Exa 才有。
- `10399b6` 清掉误提交的 0 字节 junk 文件(`169.254.169.254`、`read`)——stray shell artifact 被 stop-hook 自动提交扫进 80d085b。**教训**:自动提交会把工作树里的意外文件一起带走,跑 curl/重定向测试时当心 `>` 重定向或裸命令生成同名文件。
- `80d085b`(**自动提交,信息笼统**)= /v1/fetch **SSRF 全量加固**主体:
  - **#2 DNS-pin**:静态档 `api/fetcher.py` 改**直调 curl_cffi + CURLOPT_RESOLVE**(Scrapling 不转发 curl_options,Explore 侦察确认),`netguard.check_and_resolve()` 校验+返回钉 IP 条目,逐跳钉死。**仅直连生效**;有前向代理时 DNS 由代理接管钉失效(本机系统代理常开→本地无法演示,机制已由"黑洞 IP 钉→连接超时"证明)。
  - **#3 IPv6**:`netguard` 双栈解析(AF_UNSPEC)+ 校准 `_ip_blocked`(v6 只拦 loopback/link-local/ULA/multicast/unspecified + IPv4-mapped,刻意避开 Teredo 2001::/23 误杀)。
  - **#4 budget**:`/v1/fetch` 加整单预算,未完成 URL 记 timeout(budget_cut 入账)。
- `f45b8eb` **SSRF 加固 pt1**:#1 浏览器档抓完复校 `resp.url`+`resp.history` 落内网→丢正文返回 blocked(闭合正文外泄)。
- `35c3072` 审查 LOW #5:坏端口 `http://x:99999/` → 干净 400 `invalid_url`;`vet_url` 守 `parts.port`+`UnicodeError` 失败闭合不崩。
- `75076d0` **POST /v1/fetch**(审计最高 ROI + vonish 集成):按 URL 直取,不经搜索。`urls: str|list`(借
  OpenAI embeddings input 惯例)上限 10、去重显式入账(deduped);复用 fetch_one 全套(升级链/内容闸门/
  netguard/大小上限)。结构性错误(非 http(s)/超上限)→400;运行时拦截→item 显式状态。重定向后落点给 `final_url`。
  不变量 `requested==len(items)==Σ状态`、`submitted==requested+deduped`。实测 16/16(去重/SSRF拦内网/400/ignored_params/final_url)。
  端点后跑了 3 镜头对抗审查(wf review-v1-fetch),结果见下方"审查"节。
- `4539d1a` doc:补 design/04 第 57 行不变量漏的 `no_content` + 本记忆。
- `a514681` **搜索质量批**:词汇重排 `api/rerank.py`(标题命中≫正文,SearXNG 原分只当 prior;
  CJK 用字符 bigram)。orchestrator 在 select_candidates 前重排候选,纯噪声(标题零命中+snippet 空,
  如撤稿空壳)不进池。ResearchItem 加 `relevance`+`rank`,最终 items 先 ok 后其它、按 relevance 降序。
  /v1/search 也重排并带 relevance。实测:审计原 query "python asyncio…" 从"5 篇无关 arxiv"→"4 篇对口教程"。
- `38fb21a` **静默丢意图批**(底线②在自家 API 层):infobox/answers 透出为顶层 `answers`;
  未知/拼错参数(extra="allow")进顶层 `ignored_params` 回报;SearXNG 400→网关 400 `invalid_search_param`(不再误报 502)。
- `a865233`+`b568edb` **安全批**:`api/netguard.py` SSRF 目的地校验(内网/环回/链路本地/云元数据/保留段;
  含十进制/十六/八进制/短式/尾点等编码绕过);静态档改**手动逐跳重定向**每跳校验;20MB 大小上限;
  `api/auth.py` 共享鉴权 + `/v0` 也受保护 + hmac.compare_digest。删死代码 FETCH_PROXY/FOLLOW_REDIRECTS。
- 更早本会话:`bec9706` 内容闸门(fetch_status=ok 现在=真拿到正文,空壳→no_content,升级链渲染 SPA);
  `26eca95` mode 旋钮(fast/balanced/thorough + 池填充 + 不变量);`4471310` search/research 拆端点+多场景并集+五件套;
  `96613f3` /v1 正式面;`9f262f9` charset=utf-8。design/03/04/05 为契约权威。

## 二、37-agent 对抗审计(harvest 在 scratchpad/audit_harvest.txt 里,已停)

7 镜头 Sonnet fleet,30 条确认问题 + 26 建议。**已修**:安全批(SSRF/大小/v0鉴权/常量时间)、
静默丢意图批(infobox/extra/400→502)、搜索质量批(重排/rank)、**`/v1/fetch` 已建 + 3 镜头复审 5/5 CONFIRMED 逐条修完**。

**/v1/fetch 复审(wf review-v1-fetch)5 条全修/按拍板处置**:#5 坏端口→400(已修)、#3 IPv6(已修)、
#1 正文外泄(已修 pt1)、#2 DNS-pin(已修,直连生效)、#4 DoS/budget(已修)。**用户两次拍板**:①默认档维持
balanced(不降静态);②netguard 全量含 DNS-pin。**唯一按拍板缓的**:#1 浏览器档**抢先拦截**(挡页面内 XHR/
meta-refresh/盲 SSRF)——需注入 `page.route`,但本地缺 chromium + 重定向到内网的测试服务器无法端到端验,
俟具备条件再上(正文外泄已闭合,headline 危害已除)。

**已修(生产就绪 4 批,见交付节)**:应用日志/run_local 落盘/healthz 加深、SearXNG 上游节流/浏览器闸校准/请求限流 429、
404/405/500 封套/SSE 心跳/reconcile 假阳性、分页/域过滤/usage。

**未修(按价值排)**:
- 答案/摘要合成(需 LLM,design/06 明确故意缓做,留调用方)
- OpenAPI 未声明 Bearer 鉴权、尾斜杠 307、query 无 max_length
- 配额**强制**(需持久化存储 + 每 key 计量;本版只暴露 usage 用量)
- 残留(安全,记待办):浏览器档抢先拦截(#1 XHR/盲 SSRF,需 page.route + 测试服务器)、代理路径下 DNS-rebind
  (#2 钉只对直连生效)、流式大小提前中止

## 三、进行中/未竟

- **vonish 集成**:给了 hollow 接口文档(scratchpad/hollow-api.html,预览面板;未 publish)。`POST /v1/fetch` 已补齐,
  两跳闭环就位(search 拿 URL → fetch 取正文)。仍等用户发 vonish 工具 schema 对齐端口(vonish :18473 → hollow :8080)。
- **Explore 侦察结论(Scrapling 0.4.10 / curl_cffi 0.15,可复用)**:静态档 `Fetcher.get` **不转发 `curl_options`**
  (故 DNS-pin 只能直调 curl_cffi);浏览器档 `.fetch()` 可传 `extra_flags`(→ `--host-resolver-rules`)、`page_setup`
  回调(注册 `page.route` 抢先拦截)、`resp.history`(重定向链事后可查)。抢先拦截若要做,走 `page_setup`+`page.route`。

## 四、本会话教训
- **接近上下文极限(99%)会续写退化**:一次 Edit/commit 后空转吐 "court" 直到 Usage Policy 拦停。工作树无损(都已提交)。
  下次接近极限主动收尾 + 提示 /compact,别硬接重活。
- 服务本地跑法不变(tools\run_local.ps1);测试端 httpx 必须 trust_env=False;PowerShell 发中文用 UTF-8 字节 body。
- 用户偏好未变:重决策走 AskUserQuestion;"参考 X"=借鉴惯例非照抄;评审要小队别一次 37 只。