# 会话记忆 · Session 02 · 2026-07-06 ~ 07-07

> 首个本地会话(Windows 11,F:\Projects\HOLLOW;旧 v1 项目内容已归档至 `_archived-20260706/`)。
> 分支 `claude/new-project-setup-1dzma1`,本会话 6 个提交:ff7047d → 本次。
> 上一会话记忆:`2026-07-05-session-01.md`(云沙箱;项目本质/架构决策见那里,本文不重复)。

## 一、这个会话做了什么

1. **本地环境重验**(ff7047d):session-01 的全部 ★ 项关闭。SearXNG Windows 直跑(pwd 桩 + tzdata);
   引擎矩阵家用 IP 重测(quark/brave/mojeek 复活;被墙引擎经本地代理全部复活,无需分流);
   Scrapling 静态档恢复 `impersonate="chrome"`;浏览器档 chromium 可用。详:`docs/research/2026-07-06-local-env-verification.md`。
2. **第一个 API `/v0/research`**(a78318c):契约见 design/03;经 Opus 对抗审查修复 8 缺陷。
3. **注册表 v1.1**(7b4a91d):343 源全量落 `status`(removed 124/pool/default),五道断言闸,生成器可复现。
4. **`/v1` 正式 API 面**(96613f3):参考 OpenAI 惯例(封套/错误/SSE 语义事件/Bearer),自家域模型。
5. **search/research 拆分 + 场景体系**(4471310):`/v1/search` 纯召回;scenes 多选并集;抓取五件套
   (top_n≤20/timeout/budget/concurrency/max_content_chars);knowledge+social 场景(默认集 50)。
6. **三档升级链**(本次提交):static→dynamic→stealthy,blocked+failed 触发,默认开。

## 二、拍板记录(全部已实现)

- 默认引擎集"都来":`[duckduckgo, wikipedia, arxiv, baidu, sogou, quark]`;本地前提系统代理常开
- FastAPI ✓ / trafilatura 进 MVP ✓ / DDG 进默认(限速待做) / 中文源全放 / 应用商店留池小众 / alt-video 留池 / 密钥不投
- **"参考 OpenAI 格式"= 借鉴惯例,不是做 chat/completions 仿制层**(第一版做错已返工,教训入长期记忆)
- 纯搜索与研究**拆两个端点**;scenes 多选取**并集**、自定义引擎并入;单 URL 超时与整单 budget **分开做**;top_n 上限 8→20
- 新增 knowledge/social 场景;**zh 短期保留场景、中期升级 region 修饰符**(未做,见待办)
- 升级链:**blocked+failed 都触发**(timeout 不触发),默认开;隐身档"现在装"→实测 patchright 复用 chromium,零下载

## 三、本地实测新事实(session-01 之外)

- **Windows 坑全集**:仓库 4 个含冒号文件靠 sparse-checkout 排除 + `core.protectNTFS false`(勿清);
  `pwd` 桩 `compat/win/`;tzdata;控制台 cp932(子进程要 `PYTHONIOENCODING=utf-8`);
  **全局 HTTP_PROXY 会把 localhost 测试请求也路由进代理(+8~14s),httpx 测试端必须 `trust_env=False`**
- **SSRF 防护 × localhost 代理 = 误杀一切重定向**(curl_cffi "safe" 检查的是"这一跳连的 IP"=代理自身)
  → 走代理 `follow_redirects=True`,直连保留 "safe"(api/config.py 自动推导)
- Scrapling 静态档内置 retries=3 → 网关显式 `retries=1`;浏览器档 timeout 单位毫秒
- **StealthyFetcher 底层是 patchright(0.3.13 起替换 camoufox)**,复用 playwright 的 chromium,无需单独安装
- 经典 `google` 引擎已从上游消失(仅剩需密钥的 google cse);startpage 双环境全挂(已降出默认集)
- 引擎会被连续测试打出限流/CAPTCHA(sogou/baidu 实测),账目机制全程如实上报——正是设计要的

## 四、方法论沉淀

- **Opus 对抗审查 workflow ×4**(find 按维度并行 → 逐 finding 独立反驳复核):累计 15+4+4 候选 → 12 确认全修,
  含 high 级(resp.json() 未保护、校验错误绕过封套)。模式:每轮实现完、提交前跑一轮。
- ⚠️ **审查 agent 会污染工作树**(实测:跑生成器又 git checkout 回滚了未提交的再生成文件)——
  审未提交改动时给 agent 开 worktree 隔离,或 prompt 明令只读。
- 用户偏好:拍板/提问一律 AskUserQuestion 工具;重决策先给分析对比表。

## 五、当前 API 面(全部实测绿)

```
POST /v1/search      纯召回(query/scenes多选/engines并入)
POST /v1/research    + top_n≤20/timeout/budget(整单含搜索)/concurrency≤8/max_content_chars/escalate/purify/stream(SSE语义事件)
GET  /v1/engines     注册表直出(status/scene/type/tier 过滤)
GET  /v1/scenes      9 场景:general/knowledge/dev/academic/news/social/images/av/zh
POST /v0/research    内部面(design/03 契约,共享编排器)
```
不变量 `len(items)==requested==ok+failed+timeout+blocked` 在全部路径(含预算切断、升级链、组装异常)成立。
启动:`tools\run_local.ps1`(SearXNG :8888 + 网关 :8080)。

## 六、待办(下一会话)

1. **搜索阈值校准**(底线 4):经代理 request_timeout=5s 偏紧;今天引擎被测到限流,**隔天再校准**才有真实数据
2. **zh → region 修饰符**:注册表加 region 字段(约 10 个中文源打标),网关"场景∪该场景 zh 源"合并
3. DDG 网关侧单独限速(拍板时的附带条件,未实现)
4. 升级链的**质量触发**(200 但正文过薄时升级)——阈值需校准,与 #1 同做
5. P3:Evidence Pack 持久化(research id 目前无存储,`GET /v1/research/{id}` 留白)
6. 浏览器档常驻内存/启动耗时压测(研读遗留问题,VPS 部署前要有数)
7. **主动解 Cloudflare**:vendor solve_cloudflare 是不可中断无上限循环(审查确认会致线程泄漏),
   本版已关闭。要安全启用须让浏览器档在**独立进程**里跑(可 kill),而非线程池。

### 升级链审查(第 5 轮 Opus,8 agent)确认并已修
- **根因**:solve_cloudflare 的 vendor 循环不可中断 + run_in_executor 杀不掉线程 → 线程泄漏 →
  浏览器闸与线程池脱钩(重蹈静态档规避的排队误判)。修:①关 solve_cloudflare(线程有界);
  ②`_BROWSER_GATE` 改绑线程真实生命周期(done_callback release + shield),"持闸==持线程槽";
  ③每请求浏览器闸(防跨请求垄断);④lifespan shutdown executors(不阻塞关闭)。
